/**
 * Demo-data switch.
 *
 * false → the clinic starts CLEAN: no sample patients, appointments,
 *         leads or consent history. Only real entries created through
 *         the website (chatbot, booking form) and the admin appear.
 * true  → loads the full sample dataset (useful for demos/screenshots).
 */
export const DEMO_DATA = false;
